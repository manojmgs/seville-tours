/**
 * Test stub for the `server-only` package.
 *
 * In production `server-only` throws if a server module is pulled into a client
 * bundle. Under Vitest we import server adapters directly, so this empty module
 * stands in for it. It must never contain real logic.
 */
export {};
